# Dotfield npm test site

This is a standalone Vite consumer project for the published `dotfield@1.1.1` package.

```bash
npm install
npm run dev
```

The demo deliberately imports `dotfield` from `node_modules`, uses the package's TypeScript-ready metadata, and exercises lifecycle, interaction, mode switching, and adaptive rendering options.
